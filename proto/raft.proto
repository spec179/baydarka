syntax = "proto3";

package raft;

service Server {
  rpc GetVote(Candidate) returns (Vote) {}
}

message Candidate {
  int32 term = 1;
  int32 candidateId = 2;
  int32 lastLogId = 3;
  int32 lastLogTerm = 4;
}

message Vote {
  int32 term = 1;
  bool voteGranted = 2;
}